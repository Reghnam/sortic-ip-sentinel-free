# Skills to enable on this Bot

Enable **only** `sortic-ip-sentinel-free` from `sortic-ip-sentinel-free-grok-v0.5.28.zip` (this PR branch).

- Layout: `SKILL.md` at zip root + bundled `references/`.
- Enable **per-Bot**. Not `cp ~/.grok/skills/` (that is Grok Build).
- Do **not** Teach-by-demonstration as the install path (screen is recorded — publish).
- Do **not** enable third-party marketplace skills on the HITL Bot.
- Do **not** attach `us-ip-law-ground-truth` or any private law dump.

Scan before ship. Zip is scanned — no secrets, no corpus trees, no sidecar URLs.
